export {};

let numbers: number[] = [1, 2, 3];

let numbers2: Array<number> = [1, 2, 3];

let strings2: Array<string> = ['Tokyo', 'Osaka', 'Fukuoka'];

let strings: string[] = ['Typescript', 'JavaScript', 'Node.js'];

let a: (string | number | boolean)[] = [1, true, 'String'];
